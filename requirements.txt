# 一句话出方案 - 依赖列表
# 核心依赖：标准库为主，无需额外安装

# Python版本：>= 3.7
# 无外部依赖，使用Python标准库：
# - argparse (命令行参数)
# - re (正则表达式)
# - datetime (时间处理)
# - pathlib (路径处理)
# - sys (系统操作)
# - os (操作系统接口)

# 可选依赖（增强功能）：
# - colorama  # 彩色输出（可选，取消下行注释安装）
# colorama>=0.4.4

# 开发/测试依赖：
# - pytest>=7.0.0  # 单元测试
# - pytest-cov>=4.0.0  # 覆盖率报告
